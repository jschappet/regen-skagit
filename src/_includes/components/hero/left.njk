{% extends "components/hero/base.njk" %}

{% block heroContent %}
  <div class="mx-auto max-w-4xl px-6 md:px-8 lg:px-10">
    <div class="hero-text-bg">
      <h2 class="hero-title">{{ heroData.title }}</h2>
      <p class="hero-body">{{ heroData.description | safe  }}</p>
    </div>
  </div>
{% endblock %}
